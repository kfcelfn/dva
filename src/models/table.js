const { pathToRegexp } = require('path-to-regexp')  //路由路径正则判断

export default {
  // 命名空间 名字唯一
  namespace: 'table',

  // 状态
  state: {
   
  },

  //订阅  
  subscriptions: {
   
  },

  // 异步加载
  effects: {
   
  },

  // 修改state
  reducers: {
   
  }
  
}